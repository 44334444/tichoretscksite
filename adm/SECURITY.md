# 🔐 Безопасность админ-панели

## ⚠️ ВАЖНО

Хеш пароля теперь спрятан в файле `adm/config.js` и обфускирован с помощью base64.

## 🔑 Как изменить пароль

### Шаг 1: Создать новый хеш пароля

```javascript
// Откройте консоль браузера (F12) и выполните:
const newPassword = 'ваш_новый_пароль';
const newHash = CryptoJS.SHA256(newPassword).toString();
console.log(newHash);
// Скопируйте полученный хеш
```

### Шаг 2: Зашифровать хеш в base64

```javascript
// В консоли браузера:
const hash = 'скопированный_хеш';
const encoded = btoa(hash);
console.log(encoded);
// Скопируйте закодированное значение
```

### Шаг 3: Обновить config.js

Откройте `adm/config.js` и замените значение в функции `_AUTH_HASH`:

```javascript
window._AUTH_HASH = function() {
  return atob('ВАШ_ЗАКОДИРОВАННЫЙ_ХЕШ_ЗДЕСЬ');
};
```

## 🛡️ Рекомендации безопасности

1. **Не коммитьте config.js в публичные репозитории** с реальными паролями
   - Используйте `.gitignore` или переменные окружения

2. **Используйте переменные окружения** в продакшене:
   ```javascript
   const PASSWORD_HASH = process.env.ADMIN_PASSWORD_HASH;
   ```

3. **Никогда не используйте простой текст пароля** в коде

4. **Регулярно меняйте пароль** админа

5. **Используйте HTTPS** для передачи данных в продакшене

## 📝 Текущая конфигурация

- **Алгоритм**: SHA-256 (CryptoJS)
- **Время сессии**: 24 часа
- **Максимум попыток**: 5
- **Время блокировки**: 15 минут

## 🔄 Server-side проверка

Для максимальной безопасности рекомендуется реализовать server-side проверку пароля вместо client-side.

---

**Последнее обновление**: Январь 2026
